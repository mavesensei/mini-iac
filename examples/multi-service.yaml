containers:
  - name: api
    image: nginx:1.27
    ports:
      - "8080:80"
  - name: db
    image: postgres:16
    env:
      POSTGRES_PASSWORD: "secret"
    volumes:
      - pg-data:/var/lib/postgresql/data

network:
  name: multi-net

volumes:
  - pg-data
